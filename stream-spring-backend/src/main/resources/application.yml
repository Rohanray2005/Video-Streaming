spring:
  datasource:
    url: jdbc:mysql://localhost:3306/learn
    username: root
    password: springboot
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 1000MB
      max-request-size: 1000MB

files:
  video: videos/

file:
  video:
    hls: videos_hls/

logging:
  level:
    org:
      springframework:
        cache: DEBUG
